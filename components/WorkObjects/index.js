import React, { useState } from "react";
import { Text, View, StyleSheet } from "react-native";

function WorkObjects(props) {
    return (
        <View>
            <Text>Объекты</Text>
        </View>
    );
}

export default WorkObjects;

const styles = StyleSheet.create({});
